server:
  port: 8081
spring:
  application:
    name: validation-service
  datasource:
    password: sabanciuniv
    url: jdbc:postgresql://localhost:5432/validation
    username: sabanciuniv
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
